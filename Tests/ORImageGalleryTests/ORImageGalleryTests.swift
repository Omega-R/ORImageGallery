import XCTest
@testable import ORImageGallery

final class ORImageGalleryTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(ORImageGallery().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
